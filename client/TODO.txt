TODO – Robust Refresh & Reconnection System: Currently, refreshing the browser immediately triggers Socket.IO's disconnect, causing the server to permanently remove the player (or delete the entire room if the host refreshes), even though the user is only reconnecting. To fix this, implement a proper reconnection mechanism: generate a permanent playerId (stored in localStorage/Zustand Persist) separate from the temporary socket.id, keep socket.id in the server only, and add a rejoin-room event that runs when the investigation page loads after a refresh. Instead of instantly removing players on disconnect, mark them as disconnected and start a grace-period timer (e.g., 15–30 seconds). If the player reconnects within that window, identify them using playerId, replace the old socket.id with the new one, cancel the timer, and broadcast room-updated. Only if the timer expires should the server permanently remove the player, or delete the room if the host never reconnects. This prevents accidental room deletion due to page refreshes or brief network interruptions and provides a much more reliable multiplayer experience.
